---
# Test Inventory - Mist AI Network

all:
  vars:
    environment: test
    solution_name: mist-ai-network
    solution_abbr: mist

  children:
    mist_cloud:
      hosts:
        localhost:
          ansible_connection: local
          ansible_python_interpreter: "{{ ansible_playbook_python }}"

    juniper_switches:
      hosts:
        ex-switch-test-01:
          ansible_host: "10.100.100.1"
          switch_role: access
      vars:
        ansible_network_os: junipernetworks.junos.junos
        ansible_connection: netconf

    access_switches:
      hosts: {}
