---
# Test Inventory - SRX Firewall Platform
# Generated from configuration.csv

all:
  vars:
    environment: test
    solution_name: srx-firewall-platform
    solution_abbr: srx

  children:
    srx_firewalls:
      hosts:
        srx-test-01:
          ansible_host: "{{ srx_test_mgmt_ip | default('10.100.0.1') }}"
          cluster_node_id: 0
      vars:
        ansible_network_os: junipernetworks.junos.junos
        ansible_connection: netconf
        ansible_user: "{{ junos_user | default('admin') }}"
        ansible_password: "{{ junos_password }}"
        ansible_netconf_host_key_checking: false

    srx_standalone:
      hosts: {}

    srx_branch:
      hosts: {}
