---
# Ansible Collection Requirements for Juniper SRX
# Install: ansible-galaxy collection install -r requirements.yml

collections:
  - name: junipernetworks.junos
    version: ">=5.0.0"
  - name: juniper.device
    version: ">=1.0.0"
  - name: ansible.netcommon
    version: ">=5.0.0"
